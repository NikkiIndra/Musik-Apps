import 'package:flutter/material.dart';

class ExplolerScreen extends StatelessWidget {
  const ExplolerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
